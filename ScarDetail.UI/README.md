# ScarDetail UI

Frontend React/TypeScript do sistema ScarDetail. Consulte o [README principal](../README.md) para configuração, execução e validação do projeto completo.

```powershell
npm ci
npm run dev
```

Use `VITE_API_URL` somente quando a API não estiver acessível pelo proxy `/api` configurado no Vite.
